services:
  excalidraw:
    image: excalidraw/excalidraw:latest
    container_name: leetcode-patterns-excalidraw
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./excalidraw/libraries:/usr/share/nginx/html/custom-libraries:ro
